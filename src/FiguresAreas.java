public class FiguresAreas {
    public static void main(String[] args) {
        CalculateArea calculateArea = new CalculateArea();

        calculateArea.rectangleArea(15,20);
        calculateArea.circleArea(10);
        calculateArea.squareArea(10);
    }
}
